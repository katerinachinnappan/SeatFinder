用法：{{explore}} [api...] OPTIONS

  打开 {{API Explorer}}

  选项
    -e, --{{external}}                   打开 0.0.0.0（而非缺省 127.0.0.1）上的浏览器。这将服务器绑定到计算机上的所有 IP，并且可以在更广泛的网络上访问。

  如果提供了API，那么这些 API 将在浏览器中呈现。

  如果没有提供 API，那么将呈现当前项目中的所有 API。
